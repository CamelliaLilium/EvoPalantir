---
description: gpr 设计原则 + 外部调研/借鉴写法（含 references 强制基线 commit）
globs: rag_design/**/*
alwaysApply: false
---

# 设计原则（gpr）

撰写或评审 `rag_design/` 下方案、接口与文档时 **优先满足**：

1. **逻辑清晰严密** — 前提、推论、结论可区分；依赖 `knowledge/` 处须可指向；不偷换概念。
2. **可实施、可落地** — 每个模块能说清 **输入/输出/失败语义**；避免仅口号或仅愿景。
3. **凝练、简洁、优雅** — 能一页说清的不写十页；删重复表格与流程性空话；保留可验收句。
4. **模块高度解耦** — 边界用 **契约**（数据与事件）表达，非隐式全局状态；少跨层穿透。

**与既有规则关系：** 技术来源与禁止臆造 API 以 [rag-design-range.mdc](rag-design-range.mdc) 为准；本规则约束写法、结构与调研方法，**不放宽**事实断言。

**反例：** 无接口的架构愿景、职责堆叠无契约、把外部调研结论写进 `knowledge/` 却未标明「非规范观察」。

---

# 外部调研与借鉴写法（对齐 `EPFrameWork1/docs/references`）

范式样例见仓库内 **`EPFrameWork1/docs/references/`**（OpenClaw、OpenCode、agent-skills、lightllm-provider 等；该目录在默认 `.gitignore` 中若被忽略，以你本地克隆为准）。在 `rag_design/docs/` 下写 **同类调研** 或 **从调研提炼设计** 时遵守下列纪律。

**可复制骨架：** [rag_design/docs/references/模板.md](../docs/references/模板.md) · 目录说明：[README.md](../docs/references/README.md)

## 1. 文首必备（每条调研文档）

- **blockquote 声明**：本文 **非规范** / **不定义** EvoPalantir 或子系统正式行为；若提及本仓库设计，仅为 **观察**，不构成决策。
- **范围锚点**：日期、作者或角色；**依据** 仅限官方文档 URL、论文、标签/提交 SHA（读源码时 **写死基线提交**）。
- **一句话结论**（可选但推荐）：放在问题陈述之后，便于检索。

## 2. 推荐正文骨架（按材料深度选用）

| 阶段 | 内容 |
|------|------|
| **问题域** | 要解决什么、与 `knowledge/` 哪条链路相关。 |
| **对象界定** | 外部系统是什么、定位与同类的 **差异**（避免「像 XX」含糊）。 |
| **机制拆解** | 架构图或数据流；分节对应 **可验证** 事实（配置路径、协议名、启动顺序）。 |
| **对比** | 与 **本仓库既定设计**（`knowledge/`、宪章、需求说明）对照表；维度一致、可排序。 |
| **参考来源** | 文档链接 + 必要时 **源码文件路径** 表（「重要源码坐标」）。 |

## 3. 事实与启示 **必须分离**

- **事实段**：只陈述外部系统或可核实实现；不写「我们应」。
- **启示/借鉴段**：单独成章或加小节标题（如「观察：对 … 的启示」），并 **再次** blockquote：**非规范性观察；正式采纳须经 spec/评审；不得自动升格为 `knowledge/` 真理**。
- **差异化 / 应保持**：外部方案优点之外，明确 **本仓库不可放弃的红线**（类比 OpenClaw 文中「AgentOS 应保持的差异化」）。
- **不需要做**：列 **明确不跟进的实践**，防 scope creep（类比 agent-skills 文 §11.3）。

## 4. 源码级调研的额外义务

- 写清 **文档声称** 与 **源码实际** 的差异（加载顺序、未实现的 hook、类型占位等）。
- **未在源码中找到调用点** 须直写 **缺口**，不可写成已支持能力。
- 优先级、去重、覆盖规则等 **以源码为准** 时，注明「文档层顺序 vs 实现层顺序」。

## 5. 借鉴策略（执行顺序）

1. **机理对齐** — 外部机制对应到 **本域模块/端口**（命名可不同，语义要对）。
2. **契约映射** — 输入输出、同步/异步、幂等等 **落到接口级**，不写死未选型的实现类名。
3. **差异化守住** — 对照 `knowledge/` 与宪章，写清 **不照搬** 的点。
4. **验收句** — 借鉴后增加或保留 **可验证** 的完成标准（含负面：不做什么）。

## 6. 全景与对比表

- 多方案并列时用 **全景表**（维度 = 行或列一致：抽象层、格式、发现机制、安全模型等）。
- 「启示」表建议三列：**方向 / 行业或外部实践 / 本仓库可行动作**；动作列须 **可评审**，非口号。

## 7. 与 `knowledge/`、宪章的关系

- **规范优先级：** `knowledge/` > 已定稿需求说明 / 引擎宪章 > `rag_design` 内调研笔记。
- 调研中的「建议接口」**不得** 与宪章模块边界冲突；冲突时 **标为待决策** 并指向评审，不在调研文内私自改宪章。

## 8. `rag_design/docs/references/` 专规（**强制**）

适用于该目录下 **新建或重大修订** 的调研分析（`*.md`）：

1. **基线 commit（强制）**  
   凡分析 **有公开 Git 仓库** 的原项目，元数据区 **必须** 填写 **完整 40 位 commit SHA**（或等价：经说明的 tag + 解析出的 SHA）。**无基线 commit = 不可复查，视为不合格。**  
   **无 Git** 的材料（纯文档、论文）：必须写 **「无仓库」**，并给出 **文档 URL + 版本或抓取日期** 作为锚点。

2. **先检索、再理解、后落笔（强制）**  
   代理或作者 **必须** 使用可得的检索手段（官方文档、README、仓库文件浏览/克隆）核对 **至少一手来源**，并在文中 **「检索与阅读记录」** 列 1～3 条具体入口。**禁止** 仅凭二手摘要断言机制细节。

3. **从模板起笔**  
   新文件应自 [模板.md](../docs/references/模板.md) 复制；详见 [README.md](../docs/references/README.md)。

---

_本规则随 gpr 要求迭代；调研范式以 `EPFrameWork1/docs/references` 已落地文档为对齐样本。_
