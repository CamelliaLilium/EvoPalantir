---
description: gpr 设计原则：清晰可落地、凝练、模块解耦（rag_design / 校正记忆等）
globs: rag_design/**/*
alwaysApply: false
---

# 设计原则（gpr）

撰写或评审 `rag_design/` 下方案、接口与文档时 **优先满足**：

1. **逻辑清晰严密** — 前提、推论、结论可区分；依赖 `knowledge/` 处须可指向；不偷换概念。
2. **可实施、可落地** — 每个模块能说清 **输入/输出/失败语义**；避免仅口号或仅愿景。
3. **凝练、简洁、优雅** — 能一页说清的不写十页；删重复表格与「流程性空话」；保留可验收句。
4. **模块高度解耦** — 边界用 **契约**（数据与事件）表达，而非共享隐式全局状态；少跨层穿透。

**与既有规则关系：** 技术来源与禁止臆造 API 仍以 [rag-design-range.mdc](rag-design-range.mdc) 为准；本规则约束 **写法与结构**，不放宽事实断言。

**反例：** 冗长章节目录、重复摘要、无接口信息的「架构愿景」、把多个职责塞进同一模块且无接口切分。
